let nome = prompt( ' qual o seu nome?');
let email= prompt( ' qual o seu e-mail?');
let minuscula = email.toLowerCase();
const mensagem = ` o e-mail  usuário ${minuscula} foi cadastrado com sucesso.Seja bem-vindo(a) ${nome}`
console.log( mensagem)